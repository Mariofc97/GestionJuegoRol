<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 5.3//EN"
        "http://hibernate.org/dtd/hibernate-configuration-3.0.dtd"> 
<hibernate-configuration>
    <session-factory>

        <!-- Conexión JDBC -->
        <property name="hibernate.connection.driver_class">oracle.jdbc.OracleDriver</property>
		<property name="hibernate.connection.url">jdbc:oracle:thin:@//localhost:1522/XEPDB1</property>
        <property name="hibernate.connection.username">getafe</property>
        <property name="hibernate.connection.password">password</property>

        <!-- Dialecto de Hibernate -->
        <property name="hibernate.dialect">org.hibernate.dialect.OracleDialect</property>

        <!-- Mostrar SQL por consola -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Crear y borrar la base de datos en cada ejecución -->
        <!-- <property name="hibernate.hbm2ddl.auto">create-drop</property> -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- none(Por defecto en Hibernate 5.2+) No realiza ninguna acción sobre el esquema.
        validate:	Comprueba que las tablas y columnas existen y coinciden con las entidades. No crea ni actualiza.
        update:	 Actualiza el esquema de la base de datos para que coincida con las entidades (añade tablas o columnas si faltan). ¡No elimina!
        create:	 Borra y crea el esquema de nuevo cada vez que se inicia la app. Cuidado: se pierden los datos.
        create-drop:	Igual que create, pero además elimina las tablas al cerrar la sesión o apagar la aplicación. Ideal para tests.
        drop:	Elimina el esquema sin crearlo de nuevo (raro de usar).
        create-only:	Crea el esquema al iniciar, pero no lo elimina al cerrar (Hibernate 5.5+). -->
        <!-- Entidad mapeada -->
         <mapping class = "entities.Usuario" /> 
         <mapping class ="entities.Personaje" />
         <mapping class="entities.equipo.Equipamiento"/>
		 <mapping class="entities.equipo.objetos.Cuerda"/>
		 <mapping class="entities.equipo.objetos.Baya"/>
		 <mapping class="entities.equipo.objetos.CarneSeca"/>
		 <mapping class="entities.equipo.objetos.Comida"/>
		 <mapping class="entities.equipo.objetos.HojaParaLimpiar"/>
		 <mapping class="entities.equipo.objetos.MojonSeco"/>
		 <mapping class="entities.equipo.objetos.Palo"/>
		 <mapping class="entities.equipo.objetos.Piedra"/>
		 <mapping class="entities.equipo.objetos.Pocion"/>
		 <mapping class="entities.equipo.armas.Armas"/>
		 <mapping class="entities.equipo.armas.Arco"/>
		 <mapping class="entities.equipo.armas.Bumeran"/>
		 <mapping class="entities.equipo.armas.CanaPescar"/>
		 <mapping class="entities.equipo.armas.Cazamariposas"/>
		 <mapping class="entities.equipo.armas.Honda"/>
		 <mapping class="entities.equipo.armas.Lanza"/>
		 <mapping class="entities.equipo.armas.Trampa"/>
		 <mapping class="entities.criatura.Criatura"/>
		 <mapping class="entities.criatura.Conejo"/>
		 <mapping class="entities.criatura.Gusano"/>
		 <mapping class="entities.criatura.Mosquito"/>
		 <mapping class="entities.criatura.Raton"/>
		 <mapping class="entities.criatura.Jabali"/>		 
		 <mapping class="entities.criatura.Lobo"/>
		 <mapping class="entities.criatura.PezPrehistoricoGigante"/>
		 <mapping class="entities.criatura.Siluro"/>
		 
        
  
    </session-factory>
</hibernate-configuration>
